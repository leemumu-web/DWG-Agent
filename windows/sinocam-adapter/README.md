# SinoCAM Adapter

Status: external target contract; vendor integration is not implemented.

Adapter 目标是把平台 CAM 工作包映射到 SinoCAM 支持的输入，调用受控版本的商业软件，并将输出转换为稳定结果 schema。当前没有 SinoCAM SDK/CLI 绑定、许可配置、版本矩阵、规则实现或样本认证。

接口留白必须明确输入文件、配置版本、期望输出、超时/取消、供应商错误到平台错误的映射，以及原始供应商产物的不可变归档规则。
