# 项目领域测试

## 现有覆盖

`test_service_layer.py` 覆盖项目 CRUD、成员角色、图纸/版本、Files 关联、可见范围和跨项目拒绝；`test_sql_pagination.py` 验证 count/list、边界页和稳定排序不会遗漏/重复。

## 证据边界

输入是身份、文件和隔离数据库 fixture，输出是项目目录与访问边界证据；不执行 CAD 转换或工作流阶段。
这些测试还固定 service 分层和 SQL 分页行为，但不会证明真实大数据量索引计划或生产并发，需要数据库集成/性能验收另行覆盖。
