# 安全回归

## 现有覆盖

`test_adversarial_auth.py` 覆盖 token/cookie、枚举、锁定、伪造 claims 和越权；`test_security_boundaries.py` 覆盖路径、CORS、默认密钥、角色保护、项目/文件访问和危险输入。

## 证据边界

输入是恶意/边界请求和隔离账户，输出是已知身份、文件与项目攻击面不回退的回归证据；它不替代生产密钥管理、TLS、依赖扫描或外部渗透测试。
新增公开端点、上传格式或管理员维护动作时必须在这里或 owner 目录补越权/重放/输入边界，不得只测试成功路径。
