# Projects module

This module currently owns project-membership and global project-access rules through `access.py`. Project and drawing routes, models and services remain in their existing locations until the identity/projects vertical-slice migration. This transitional boundary is executable and must not be described as a completed domain move.
