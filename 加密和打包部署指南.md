# 加密和打包部署指南

本文说明 DWG Agent 当前正式发布方式，适用于 r32 及后续版本。目标是：

- 服务器不需要仓库源码即可运行；
- 发布包在传输和存放时保持加密；
- 更新应用时保留 MySQL、MinIO、账号、项目和业务文件；
- 通过统一脚本完成镜像审计、分层启动和上线检查。

## 一、发布物和保护边界

正式发布包含四类固定版本镜像：

- `dwg-agent-backend:<版本>`：API、各类 worker 和业务算法；
- `dwg-agent-frontend:<版本>`：前端和 Nginx；
- `dwg-agent-mysql:<版本>`：MySQL；
- `dwg-agent-minio:<版本>`：MinIO。

后端镜像使用 `backend/Dockerfile` 的 `protected` 目标构建。构建过程先编译业务 Python 字节码，再从最终镜像中删除业务 `.py` 源码、测试、样本和开发文件。发布脚本还会检查关键模块能否导入、数据库迁移是否只有一个 head、ODA 双向转换是否可运行，并逐层检查导出的后端镜像。

最终发布包为：

```text
dwg-agent-<版本>.tar.gz.gpg
dwg-agent-<版本>-deploy.sh
dwg-agent-<版本>.tar.gz.gpg.sha256
```

`.gpg` 文件使用接收者公钥加密，只有持有对应私钥的机器能够解密。运行密码、数据库内容、对象文件和服务器 `.env.docker` 不会进入发布包。

这套方式用于防止源码被直接浏览，并保护发布包的传输和静态存放。拥有服务器 root 权限的人仍可分析镜像和字节码，因此不能把它理解为对服务器最高权限人员的绝对防破解。

### 1.1 整体保护结构

当前发布方式不是只做一次“文件加密”，而是把不同目标交给不同保护层：

| 保护层 | 解决的问题 | 当前实现 |
| --- | --- | --- |
| 发布包机密性 | 防止没有私钥的人直接读取传输包 | GPG 公钥加密 |
| 业务源码降可读 | 防止进入容器后直接浏览 Python 源码 | 编译字节码，最终镜像不携带业务 `.py` |
| 发布物完整性 | 发现文件损坏、缺失或镜像不一致 | 包内外 SHA-256、镜像 ID 清单 |
| 发布物真实性 | 确认发布包确实来自指定签名者 | 可选的 GPG 分离签名 |
| 运行时隔离 | 限制容器对主机和文件系统的修改能力 | 非 root 用户、只读根文件系统、能力删除 |
| 秘密与数据隔离 | 防止运行密码和生产数据混入发布包 | `.env.docker` 与 Docker 数据卷独立保存 |

这些保护层不能互相替代。例如，SHA-256 能发现文件是否变化，但不能隐藏文件内容；字节码处理能提高源码阅读和逆向门槛，但不能保护传输中的发布包；GPG 加密能隐藏发布包内容，但不能证明运行时容器没有越权。

完整链路如下：

```text
仓库源码
  │
  ├─ 构建前端生产静态文件
  │
  └─ 编译后端业务字节码并删除业务源码、测试和样本
          │
          ▼
     四个固定版本 Docker 镜像
          │
          ├─ 镜像运行检查
          ├─ ODA 双向转换检查
          └─ 镜像逐层源码检查
          │
          ▼
 images.tar + Compose + 部署脚本 + 无秘密配置模板
          │
          ├─ 生成包内 SHA-256 清单
          ├─ tar 归档
          ├─ gzip 压缩
          └─ GPG 公钥加密
          │
          ▼
 dwg-agent-<版本>.tar.gz.gpg
          │
          ├─ 生成包外 SHA-256
          └─ 可选生成 GPG 分离签名
          │
          ▼
 服务器用私钥解密、校验并载入 Docker
          │
          ▼
 使用服务器原有 .env.docker 和数据卷启动
```

### 1.2 GPG 为什么既能安全传输，又不需要在构建机保存服务器密码

发布脚本实际执行的核心过程是：

```text
tar → gzip -9 → gpg --encrypt --recipient <接收者公钥>
```

GPG 对大文件通常采用混合加密，基本过程是：

1. 构建机临时生成一个随机的对称会话密钥；
2. 使用该会话密钥加密压缩后的完整发布载荷；
3. 使用接收者公钥加密会话密钥；
4. 把“被加密的会话密钥”和“被加密的发布载荷”一起写入 `.gpg` 文件；
5. 服务器使用对应私钥解开会话密钥，再用会话密钥解开发布载荷。

这样设计有两个实际优点：

- 大体积镜像数据由高效的对称算法处理，适合数 GB 的离线发布包；
- r36 起，发布脚本要求受控构建机临时持有对应私钥，并在密文写完后立即执行一次完整解密与 tar 可读性验证；服务器运行密码仍不参与构建。

公钥可以分发给普通构建节点。执行正式发布的受控构建机和部署端必须能访问私钥，用于发布后自检和实际部署；私钥不能进入仓库、镜像、发布包、Compose 配置或命令记录，发布完成后应回到受限密钥环或离线介质。

发布脚本没有固定声明某一种对称算法，而是由当前 GPG 版本和接收者密钥能力协商。因此运维文档不应把它写死为某个 AES 变体；实际算法需要通过 GPG 包信息和本机策略核验。

需要明确：

- 只有公钥不能解密发布包；
- 私钥泄露后，攻击者可以解密所有使用该公钥生成且被其取得的发布包；
- 加密只保护内容机密性，不会自动证明发布者身份；
- 当前命令使用一个接收者。若未来需要多台独立部署端，应明确使用多个接收者或分别生成发布包。

### 1.3 加密、签名和 SHA-256 的区别

这三个概念经常被混在一起，实际作用不同：

| 机制 | 隐藏内容 | 发现内容变化 | 证明发布者身份 |
| --- | --- | --- | --- |
| GPG 公钥加密 | 是 | 加密格式自身可发现部分破坏，但不能替代发布清单 | 否 |
| SHA-256 清单 | 否 | 是 | 否 |
| GPG 分离签名 | 否 | 是 | 是，前提是签名公钥已被可信验证 |

当前流程有两层 SHA-256：

1. **包内清单 `SHA256SUMS`**：记录解密后所有发布文件的哈希值。部署器解密到临时目录后逐项校验，能发现内部文件损坏、缺失或被替换；
2. **包外 `.sha256` 文件**：记录加密包和独立部署脚本的哈希值，能在解密前发现传输不完整或文件不一致。

SHA-256 文件本身如果与发布物一起被恶意替换，单独依靠哈希无法识别攻击者。因此，对发布来源有强真实性要求时，构建命令应增加：

```bash
--signing-key <受控签名私钥>
```

这会为加密包生成 `.asc` 分离签名。部署器发现该文件时会执行 GPG 验签。验签有效的前提是部署机已经通过受控渠道确认签名公钥指纹；仅仅“能够导入某个公钥”不代表该公钥可信。

当前分离签名覆盖加密包本身，不覆盖独立部署脚本和 `.sha256` 文件。对更高安全等级的环境，部署脚本也应从受控软件仓库取得，或额外纳入独立签名和发布审批流程。

### 1.4 后端源码如何降低可读性

受保护后端镜像采用独立的多阶段构建，不是在带源码的最终镜像中简单执行“删除文件”。

构建过程分为：

1. `runtime` 阶段收集后端、数据库迁移和各业务 Stage 的完整运行文件；
2. `bytecode-compiler` 阶段使用 `compileall` 把业务 Python 编译为 `.pyc`；
3. 编译时使用 checked-hash 模式，使字节码与对应源码哈希建立确定关系；
4. 编译路径从构建时 `/app` 映射为逻辑路径 `/opt/dwg-agent`，避免字节码记录无意义的构建机路径；
5. 删除业务 `.py`、类型存根、测试、样本、数据、日志、开发配置和文档；
6. `protected` 最终阶段从不含业务源码的 `runtime-base` 重新开始，只复制已经清理的运行树。

最后一步非常重要。Docker 镜像由多个不可变层组成：如果先把源码复制进最终镜像，再在后续层删除，源码仍可能存在于历史层中。当前最终 `protected` 阶段只复制清理后的文件，因此不会因为“后层删除、前层仍在”而把源码留在最终镜像历史中。

发布脚本还会把后端镜像导出为归档，并检查该镜像的每一个实际层。只要 `/app/app`、`/app/migrations` 或 `/app/Stages` 下任一历史层仍存在业务 `.py`，发布就失败。

字节码处理的准确安全边界是：

- 能阻止普通使用者通过 `docker exec` 或导出镜像直接阅读原始 Python 源码；
- 能移除测试、样本和开发文档中可能泄露的业务细节；
- 能显著提高分析成本；
- 不能阻止专业人员反编译 `.pyc`、跟踪运行行为或读取进程内存；
- 不能替代代码签名、主机权限控制、磁盘加密和网络隔离。

因此指南统一使用“受保护镜像”“源码降可读”或“提高逆向门槛”，不使用“绝对加密源码”或“无法破解”。

### 1.5 为什么还要做镜像运行检查

删除源码、测试和工具文件后，最常见风险不是加密失败，而是把生产运行需要的资源也删掉。为防止出现“开发环境正常、加密镜像缺功能”，发布脚本会在只读、无网络的临时容器中检查：

- 业务 `.py` 源码数量必须为零；
- Excel 样本和 Stage 测试目录必须为零；
- 容器必须以非 root 用户运行；
- API、Excel、BH 左右进依赖、DWG/DXF 转换、分类、拆板和余料读取模块可以导入；
- 数据库迁移只能有一个 head；
- 主要 CLI 能启动；
- NumPy CPU 指令基线符合目标服务器兼容要求；
- ODA 能完成一次真实的 DXF→DWG→DXF 双向转换；
- 拆板发布证明可以载入。

这些检查验证的是“受保护后的实际镜像”，不是构建前源码环境。只有它们全部通过，镜像才会进入加密发布包。

### 1.6 运行密码和业务数据为什么不进入发布包

发布包只携带无秘密的 `.env.docker.example`，不携带实际 `.env.docker`。服务器安装时：

- 首次安装生成环境文件模板，由管理员在服务器上填写秘密；
- 无损升级保留服务器原有 `.env.docker`；
- 发布镜像不保存数据库密码、MinIO 密码、JWT 密钥或初始管理员密码；
- MySQL 数据、MinIO 对象和应用工作文件保存在独立 Docker 数据卷中；
- 新镜像只替换程序，不替换数据卷。

因此，同一个加密发布包可以在不同服务器上使用不同运行密码，而构建者不需要知道生产秘密。

这也意味着：

- GPG 只加密离线发布包，不加密 MySQL 或 MinIO 数据卷；
- 发布包解密并执行 `docker image load` 后，Docker 本地镜像不再由该 `.gpg` 文件保护；
- 部署器的解密临时目录会在受控流程结束时清理，但 Docker 镜像仍需依靠服务器权限保护；
- 若要求服务器关机状态下的磁盘机密性，需要另行配置 LUKS、硬件加密盘或云磁盘加密；
- 若要求数据库字段级加密或 MinIO 对象服务端加密，也必须单独设计，不能依赖发布包 GPG。

### 1.7 运行时容器的隔离措施

正式 Compose 对应用容器使用以下限制：

- 以非 root 用户运行；
- 根文件系统只读；
- 删除 Linux capabilities；
- 启用 `no-new-privileges`；
- 只为 `/tmp`、用户临时目录和必要工作卷提供有限写空间；
- MySQL、MinIO 和应用服务通过内部网络通信；
- 容器日志限制大小和保留数量；
- 单个进程异常退出后按 `unless-stopped` 策略恢复。

这些措施用于缩小运行时故障或入侵后的影响范围。它们不能防御主机 root，也不能弥补弱密码、暴露的 Docker Socket、错误的端口开放或泄露的私钥。

### 1.8 密钥管理要求

GPG 加密的最终安全性取决于私钥管理：

1. 为正式发布建立专用 GPG 密钥，不与个人邮件签名密钥混用；
2. 在不同渠道核对完整公钥指纹，不能只看姓名或短 Key ID；
3. 私钥只保存在授权部署机或离线介质中，并限制文件权限；
4. 私钥备份应加密保存，并与服务器分开放置；
5. 不在 shell 命令、聊天、工单、Git、镜像或普通日志中输出私钥和口令；
6. 定期检查密钥有效期、吊销证书和可恢复性；
7. 人员或服务器权限变化时及时轮换接收者密钥；
8. 怀疑私钥泄露时立即停止使用旧公钥、生成新密钥、撤销旧密钥，并重新评估历史发布包暴露范围。

更换 GPG 密钥不会自动重新加密历史发布包。需要继续保存的历史包应使用新公钥重新生成，或放入另一个受控加密存储中。

### 1.9 能防什么，不能防什么

| 场景 | 当前方案的效果 |
| --- | --- |
| 发布包在 U 盘、网络或普通文件服务器上被未授权人员取得 | 没有私钥时不能直接解密 |
| 传输中丢字节或文件损坏 | 包外和包内 SHA-256 可以发现 |
| 普通服务器使用者进入容器浏览 Python 源码 | 最终镜像没有业务 `.py`，只能接触字节码 |
| 发布镜像历史层意外残留源码 | 逐层归档审计会阻止发布 |
| 错把测试、样本或开发资料打入镜像 | 发布前检查会阻止发布 |
| 伪造发布包和哈希文件 | 仅靠 SHA-256 不能防；需要可信 GPG 签名 |
| 攻击者取得服务器 root 权限 | 不能可靠防御其导出镜像、读取字节码、内存和数据卷 |
| GPG 私钥泄露 | 不能阻止攻击者解密其取得的相关发布包 |
| 服务器磁盘被离线读取 | 发布包仍加密，但已加载镜像和数据卷需要额外磁盘加密 |
| 数据库密码弱或 Web/API 存在漏洞 | 发布包加密不能解决，必须依靠身份、权限和应用安全 |
| MySQL、MinIO 业务数据需要静态加密 | 当前发布机制不提供，需另行配置数据存储加密 |

## 二、发布前准备

在仓库根目录操作：

```bash
cd /home/Creeken/Paper/CAD_research/complete_framework
```

确认代码、测试和发布配置已通过：

```bash
bash scripts/verify.sh quick
git status --short
```

`git status` 中不得有尚未确认的业务代码修改。数据目录、临时输出和历史发布包不应加入提交。

确认 Docker、GPG、接收者公钥和本机受限密钥环中的对应私钥可用：

```bash
docker version
gpg --list-keys
gpg --list-secret-keys <完整发布密钥指纹>
```

接收者参数必须使用专门发布密钥的完整指纹。发布脚本会拒绝过期公钥、无本地私钥的接收者以及无法立即解密验证的密文。不得把私钥、密码或令牌写入仓库、命令历史、发布说明或镜像。

版本名必须唯一且不可变，建议格式：

```text
server-production-YYYYMMDD-rNN
```

同一版本名不得重复构建或覆盖。代码变化后必须提升版本号。

## 三、生成受保护镜像和加密发布包

执行：

```bash
bash scripts/release.sh bundle \
  --recipient <发布公钥指纹或长Key-ID> \
  --output releases \
  --version server-production-YYYYMMDD-rNN
```

脚本会依次完成：

1. 检查 Docker 部署配置；
2. 构建受保护后端镜像和前端生产镜像；
3. 为 MySQL、MinIO 和应用镜像创建固定版本标签；
4. 检查最终后端镜像中没有业务 `.py` 源码、测试和 Excel 样本；
5. 执行关键模块导入、迁移 head、CLI 和 ODA 双向转换烟测；
6. 生成只引用固定镜像的 `compose.server.yaml`；
7. 导出镜像并逐层审计；
8. 生成内部 SHA-256 清单；
9. 使用 GPG 加密完整发布包；
10. 立即用对应私钥解密并遍历 tar，验证密文、压缩流和归档结构完整；
11. 生成独立部署脚本和外部 SHA-256 文件。

只有命令以零退出码结束，并明确输出“encrypted server release created”，发布物才算生成成功。

如镜像已经由同一提交构建和验证，可使用：

```bash
bash scripts/release.sh bundle \
  --recipient <发布公钥指纹或长Key-ID> \
  --output releases \
  --version server-production-YYYYMMDD-rNN \
  --skip-build
```

`--skip-build` 只能用于已经确认标签和代码完全对应的镜像，不能用于跨提交复用。

## 四、首次部署到服务器

将以下三个文件传到服务器同一目录：

```text
dwg-agent-<版本>.tar.gz.gpg
dwg-agent-<版本>-deploy.sh
dwg-agent-<版本>.tar.gz.gpg.sha256
```

安装示例：

```bash
chmod 0755 dwg-agent-<版本>-deploy.sh
./dwg-agent-<版本>-deploy.sh install \
  dwg-agent-<版本>.tar.gz.gpg \
  /opt/dwg-agent/server
```

安装程序会先校验外部 SHA-256，再解密发布包、校验内部清单、加载镜像并核对镜像 ID。首次安装还会生成：

```text
/opt/dwg-agent/server/.env.docker
```

打开该文件，替换所有 `CHANGE_ME_*` 项，并至少确认：

- MySQL 与 MinIO 密码足够复杂且互不复用；
- JWT、签名和内部服务密钥为随机高强度值；
- `HTTP_PORT` 是计划开放的端口；
- `APP_ENV=production`；
- Excel 第二阶段为常开能力，无需也不得另设启停开关；
- 生产所需的 DWG、分类、拆板、Excel 和余料功能门已启用；
- worker 并发值符合服务器实际 CPU、内存和 I/O 能力；
- 最大批量图纸数为当前业务约定值。

环境文件权限必须保持为 `0600`：

```bash
chmod 0600 /opt/dwg-agent/server/.env.docker
```

不要把 `.env.docker` 复制回仓库或加入发布包。

### 4.1 24 vCPU / 64 GiB 的生产资源基线

当前正式模板把 CPU 作为主要调度边界，内存只设置宽松的防失控上限。已验证的固定并发为：DWG→DXF 4、DXF→DWG 2、分类 2、拆板 1、Excel 第一阶段 1、Excel 第二阶段 1、余料转换 1、余料解析 2、报告 1。拆板和 Excel 第二阶段保持单任务执行，避免多个重型项目同时放大 CPU、临时文件和数据库写入压力。

主要容器 CPU 上限为：API 4 核、DWG→DXF 6 核、DXF→DWG 3 核、分类 2 核、拆板 2 核、Excel 两阶段各 2 核、MySQL 4 核、MinIO 2 核。正式 64 GiB 主机仍使用模板中的 2–8 GiB 单容器内存上限；这些上限不是预分配，容器只使用实际需要的内存。

所有 Celery worker 使用独立业务队列、`prefetch=1` 和晚确认。worker 并发已满或暂时停止时，新任务应保持在 MySQL-backed broker 和 Job 账本的 `queued` 状态；资源恢复后按队列继续处理。任务入队后只依赖持久化的 Job、文件和项目身份，不依赖浏览器会话，因此用户退出登录不会取消后台处理。任务完成后应立即清理已消费 broker 行；worker 异常退出时由启动清理和陈旧任务恢复机制接管。

不得为了利用 64 GiB 内存而盲目增加 CPU 密集 worker。调整并发前至少用两个不同账号同时提交真实项目，并确认：

- 分类与拆板同时运行时容器没有重启或 OOM；
- 资源不足时出现可恢复排队，而不是重复提交或丢任务；
- 每阶段输入数等于成功、人工复核和失败口径下的完整输出数；
- 退出登录后任务继续，重新登录可查看同一 Job 终态；
- `kombu_message` 和 active jobs 在全部任务结束后归零；
- MySQL 文件记录对应的 MinIO 对象大小和 SHA-256 一致。

## 五、启动顺序和健康检查

正式启动：

```bash
/opt/dwg-agent/server/scripts/server-deploy.sh up \
  /opt/dwg-agent/server
```

脚本固定按以下顺序恢复：

1. MySQL、MinIO；
2. backend API；
3. Nginx 和全部 worker；
4. 网关、数据库、对象存储和余料库真实回读烟测。

这样可以避免 API 或 worker 在数据库尚未就绪时反复崩溃。

查看状态：

```bash
/opt/dwg-agent/server/scripts/server-deploy.sh status \
  /opt/dwg-agent/server
```

当前完整生产配置应有 15 个服务，所有服务均应为 `running`，配置了健康检查的服务均应为 `healthy`。其中包括独立的 `worker-excel-stage2`。

再次执行正式烟测：

```bash
/opt/dwg-agent/server/scripts/server-deploy.sh smoke \
  /opt/dwg-agent/server
```

烟测会检查：

- Nginx 网关；
- API、MySQL 和 MinIO 就绪状态；
- 受保护运行时中的余料 DXF 上传、登记、解析、查询、预览和清理。

## 六、启用开机恢复和崩溃自动重启

首次部署成功后，以 root 身份执行：

```bash
/opt/dwg-agent/server/scripts/server-deploy.sh enable-service \
  /opt/dwg-agent/server
```

该命令安装并启用 `dwg-agent.service`。服务器启动时会在 Docker 和网络就绪后按正确顺序恢复容器；启动失败会延迟重试。单个容器的主进程异常退出时，由 Compose 的 `unless-stopped` 策略自动恢复。

检查：

```bash
systemctl is-enabled docker
systemctl is-enabled dwg-agent.service
systemctl status dwg-agent.service
```

管理员主动执行 `docker stop`、`docker kill` 或 Compose `down` 属于“明确停止”，不能用来证明自动重启能力。

## 七、从旧版本无损升级

升级前必须确认没有正在排队或运行的生产任务。

先安装新发布包到原发布目录：

```bash
./dwg-agent-<新版本>-deploy.sh install \
  dwg-agent-<新版本>.tar.gz.gpg \
  /opt/dwg-agent/server
```

安装程序会：

- 替换发布目录内的 Compose、初始化 SQL、部署脚本和镜像清单；
- 保留原有 `/opt/dwg-agent/server/.env.docker`；
- 不删除 MySQL、MinIO 或应用数据卷；
- 不重建账号和业务数据。

然后执行：

```bash
/opt/dwg-agent/server/scripts/server-deploy.sh recover \
  /opt/dwg-agent/server
```

恢复完成后必须核对：

```bash
/opt/dwg-agent/server/scripts/server-deploy.sh status \
  /opt/dwg-agent/server

/opt/dwg-agent/server/scripts/server-deploy.sh smoke \
  /opt/dwg-agent/server
```

严禁在升级中执行：

```text
docker compose down -v
docker volume rm ...
docker system prune --volumes
```

这些命令可能永久删除数据库和对象文件。

## 八、本机 18080 端口发布验证

r32 及后续本机验证沿用正式受保护镜像，只通过临时 Compose 覆盖设置端口和本机并发。临时覆盖文件不得包含明文密码，也不进入仓库。

覆盖配置至少应做到：

- Nginx 映射 `18080:8080`；
- 四类镜像都固定到同一个发布版本；
- `APP_ENV=production`；
- Excel 第二阶段由正式模板和专用 worker 常开，不依赖环境开关；
- 新增并启用 `worker-excel-stage2`；
- 保持最大 5000 张图纸约束；
- API 与全部 worker 使用一致的功能门；
- 继续挂载原 `dwg-agent_mysql_data`、`dwg-agent_minio_data` 和 `dwg-agent_app_var`。

启动时使用原 Compose 项目名，才能复用原数据卷：

```bash
docker compose \
  --project-name dwg-agent \
  --project-directory . \
  --env-file .env.docker \
  -f compose.yaml \
  -f /tmp/dwg-agent-<版本>-18080.override.yaml \
  --profile workers \
  up -d --no-build --remove-orphans
```

如果本机密码只存在于正在运行的旧 API 容器，可在同一条受控 shell 中只读提取并临时导出；不得打印或写入 `/tmp` 文件。切换完成后立即清除 shell 变量。

本机检查：

```bash
curl -fsS http://127.0.0.1:18080/nginx-health
curl -fsS http://127.0.0.1:18080/health/ready
docker compose --project-name dwg-agent ps --all
```

还必须在真实浏览器中检查：

- 登录可用，原账号仍存在；
- 工作流页面可以打开；
- Excel 第一阶段仍可使用；
- Excel 第二阶段显示“处理 BH 的左右进”，不再显示“等待上线”；
- 第二阶段预检、执行进度和两份 Excel 下载入口状态正确；
- 余料库、DWG/DXF 转换、分类和拆板入口仍可用；
- 前端只显示面向操作人员的中文错误，不泄露后端日志。

## 九、数据保护核验

升级前后记录数据卷名称：

```bash
docker volume ls
docker inspect dwg-agent-mysql-1
docker inspect dwg-agent-minio-1
```

本机标准数据卷为：

```text
dwg-agent_mysql_data
dwg-agent_minio_data
dwg-agent_app_var
```

升级后应核对：

- MySQL、MinIO 容器仍挂载同名卷；
- 原账号、角色和权限没有变化；
- 原项目、工作流、文件、Job 和余料记录仍可查询；
- MinIO 中原对象仍可下载；
- 新版本没有创建第二套空数据库；
- API、worker、MySQL 和 MinIO 属于同一个 Compose 项目和内部网络。

镜像可以删除或替换，数据卷不能随镜像清理。

## 十、回滚

只有数据库迁移兼容旧版本时，才允许镜像回滚。回滚步骤：

1. 确认没有运行中的任务；
2. 保留现有数据卷；
3. 把 Compose 固定镜像改回上一正式版本；
4. 按 MySQL/MinIO、API、其余服务的顺序执行 `recover`；
5. 重新执行状态检查、烟测、登录和关键下载验证。

回滚不是恢复数据库备份。若新版本执行了旧版本不兼容的数据库迁移，必须使用事先制定的数据库迁移回退方案，不能仅切换镜像。

## 十一、常见问题

### 页面仍显示“等待上线”

先确认浏览器访问的是新 Nginx 容器，再确认：

- 前端镜像标签是新版本；
- API 和 `worker-excel-stage2` 均使用新后端镜像；
- 系统健康页将 Excel 第二阶段标记为已启用（该能力没有启停开关）；
- `worker-excel-stage2` 已运行且健康；
- 浏览器强制刷新后加载的是新静态资源。

只更新后端或只更新前端都不能完成 Excel 第二阶段上线。

### 服务数量少于 15

通常是启动时没有启用 `workers` profile，或旧 Compose 配置没有 `worker-excel-stage2`。应使用当前发布包内的 `compose.server.yaml`，并重新执行 `recover`。

### 更新后出现空数据库

立即停止继续写入，检查 Compose 项目名和 MySQL/MinIO 卷挂载。最常见原因是使用了不同项目名，导致 Docker 自动创建了新的空卷。不要初始化、复制或删除任何卷，先恢复原项目名和原挂载关系。

### 加密包无法安装

依次检查：

- `.sha256` 文件是否与加密包位于同一目录；
- 文件是否传输完整；
- 当前机器是否持有对应私钥；
- GPG 是否能识别该私钥；
- 磁盘空间是否足以同时存放加密包、解密镜像包和 Docker 镜像。

### 容器反复重启

先查看具体容器日志和健康状态，重点检查 MySQL、MinIO、环境变量、数据库迁移和功能门。不得通过不断重建数据库来掩盖配置错误。

## 十二、发布完成判定

同时满足以下条件才算完成：

- 仓库测试和前端生产构建通过；
- 受保护镜像审计、关键模块导入和 ODA 烟测通过；
- GPG 加密包与双层 SHA-256 清单生成成功；
- 15 个服务全部运行，健康检查通过；
- 现有账号、项目、数据库和对象文件无丢失；
- 浏览器实际加载新版本；
- Excel 第二阶段真实显示并可提交 BH 左右进任务；
- Stage2 专用 worker 能消费任务；
- 两份 Excel 产物可分别下载；
- 余料库、转换、分类和拆板等既有功能没有因加密镜像缺文件而退化；
- 发布版本、镜像标签、Compose 配置和验收记录彼此对应。
