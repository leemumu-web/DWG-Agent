{
  adminName: "${CB_ADMIN_NAME}",
  adminPassword: "${CB_ADMIN_PASSWORD}",
  teams: [
    {
      subjectId: "dba-admin",
      teamName: "DWG database administrators",
      description: "Platform administrators using the scoped write connection.",
      permissions: []
    },
    {
      subjectId: "dba-reader",
      teamName: "DWG database readers",
      description: "Authenticated platform users using the read-only connection.",
      permissions: []
    }
  ]
}
