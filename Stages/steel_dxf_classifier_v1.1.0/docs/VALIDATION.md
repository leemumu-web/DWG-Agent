# 发布验证记录

本文记录 Steel DXF Classifier 1.2.0 对既有真实 Tekla DXF 语料的回归基线。原始 ZIP 保留不变，先完整解压，再对目标目录第一层的 DXF 执行分类；解压目录、分类目录、`STEEL-DXF-CLASSIFICATION-1.2` JSON 和 CSV 均作为中间结果保留。

## 真实项目结果

| 项目 | 输入数 | 自动分类 | 待确认 | 无法读取 | 类型分布 | 实测耗时 |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| 项目1 | 72 | 72 | 0 | 0 | BH 67、BOX 2、PL 3 | 15.155 s |
| 项目2 | 171 | 171 | 0 | 0 | BH 141、BOX 30 | 43.265 s |

两组共 243 张 DXF。每张图均只形成 1 个标题栏规格候选，诊断均为 `TITLE_PROFILE_PROVED`；没有依靠文件名或材料表游离文本补判。

## 完整性检查

- 项目1：`input=72 routed=72 byte_mismatch=0`；
- 项目2：`input=171 routed=171 byte_mismatch=0`；
- 每个输出文件都能按文件名对应到输入目录中的唯一源文件；
- 使用逐文件二进制比较确认复制内容未改变；
- 报告汇总数等于各分类目录 DXF 数之和；
- 项目1：待确认 0、无法读取 0；项目2：待确认 0、无法读取 0。

## 拆板前文件名迁移

2026-07-18 对两个真实输入目录第一层执行了原地预处理，随后用 `--overwrite` 重建全部分类目录和报告。原始 ZIP、项目1的备份目录和项目2的 DWG 目录未修改。

- 项目1：72/72 个输入、72/72 个输出以 `_拆板前.dxf` 结尾；
- 项目2：171/171 个输入、171/171 个输出以 `_拆板前.dxf` 结尾；
- 两个项目均不存在 `_拆板前_拆板前.dxf`；
- JSON 中 243 个 `source_name` 全部使用新名称；
- 项目1迁移前后内容集合摘要均为 `a6e998270a4f206af08677a6725c94799221360e08eba492f40591bec36842d6`；
- 项目2迁移前后内容集合摘要均为 `e5049fd33e6636fc4076069bf35fc3c00f60ada17e85b8c920f2a5d1f4f7011d`。

内容集合摘要的计算只取每张 DXF 的 SHA-256，再排序汇总，因此不受文件名变化影响。摘要一致证明迁移过程中 DXF 字节内容保持不变。

## 1.2 JSON 流与类型语义验证

两个真实项目的数量和字节摘要继续作为回归基线。1.2 自动化测试验证 stdout 只产生一个 `STEEL-DXF-CLI-1.2` JSON 对象，且对象中的 `summary` 与同次生成的 `STEEL-DXF-CLASSIFICATION-1.2` 文件报告汇总一致。

- 项目1：`status=completed`，输入 72，BH 67、BOX 2、PL 3；
- 项目2：`status=completed`，输入 171，BH 141、BOX 30；
- 两个项目的待确认和无法读取均为 0。

真实数据全部自动通过不代表分类器会强制给出结果。单元测试另行覆盖字段缺失、候选冲突、损坏 DXF、中文转义、嵌套块、已有输出保护与事务回滚等失败路径；这些情况分别进入 `待确认`、`无法读取` 或以退出码 1 停止。

1.2 类型语义回归另外覆盖 PX 目录类型、安全新前缀的自动发现、`type_source=auto_discovered`、稳定 `group_key` 和 `next_stage_eligible`。上述两组历史真实项目不包含 PX，表中的分布不用于冒充 PX 实物图验收；PX 与动态类型由构造的标题栏 DXF 自动化样本验证。

## 可复核产物

每个项目的解压目录中保留：

```text
<项目名称>/
├── <项目名称>_dxf/             # 原始分类输入
├── <项目名称>_<类型>_dxf/      # 原样复制的分类结果
├── <项目名称>_分类清单.csv
└── <项目名称>_分类报告.json
```

发布 ZIP 包含上述目录和原压缩包中已有的备份/DWG 目录。JSON 是证据审计的权威记录，CSV 用于人工筛选，不能只看文件夹名称替代报告验收。
