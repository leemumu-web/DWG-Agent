# 工作台

## 现有实现

`DashboardPage.tsx` 查询系统健康、文件与 Job 汇总，展示运行概览、最近任务以及进入项目、生产流程和文件管线的快捷入口；`styles.css` 负责响应式指标布局，`index.ts` 对外导出页面。

## 业务流与边界

输入是 system/files/jobs 的公开查询和当前权限，输出是操作员的工作起点。卡片数字是后端当前快照，不承担写操作、任务恢复或数据库统计真相。
查询失败时页面保留其他可用入口并展示可诊断错误，不用零值掩盖服务异常；面向管理员的深度队列/对象数据仍归 operations 控制台。
