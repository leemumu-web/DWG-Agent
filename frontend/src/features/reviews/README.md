# 人工复核

## 现有实现

`ReviewsPage.tsx` 展示待处理/已决复核记录、关联 Job/Result、结论与意见输入；`reviews.api.ts` 调用列表和决策端点；`index.ts` 暴露路由页面。

## 业务流与边界

输入是 jobs 域生成的 review record、结果和授权用户，输出是后端持久化的可审计人工决策。该页面不修改 CAD/Excel 原始结果，也不代表自动拆板核心已实现。
重复提交、已决记录或无权限响应必须原样展示后端状态，不在浏览器中乐观伪造结论；实际待办范围由服务端项目访问过滤。
