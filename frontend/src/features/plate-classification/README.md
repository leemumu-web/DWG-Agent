# plate-classification — 板件图形分类前端

板件图形分类（异孔折判断）的前端面板，用于触发拆板后 DXF 目录的分类任务并查看历史运行与条目结果。

## 边界

- 本目录只负责展示与交互，不实现分类算法，也不能直接访问后端数据库；所有数据通过 `workflows.api.ts` 暴露的 HTTP 接口读写。
- 该面板受功能开关保护，未开启时不得渲染或发起请求。

## 源文件

- `index.tsx` — 触发表单、运行列表与结果展示面板
