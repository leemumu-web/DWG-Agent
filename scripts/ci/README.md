# 持续集成脚本

## 职责

`write_env.py` 仅在 CI 中生成本次运行专用、权限为 `0600` 的随机环境文件；`run_container_validation.sh` 使用唯一 Compose 项目和卷验证受保护镜像、ODA、完整服务、MySQL/MinIO、余料链路与浏览器回归，并在退出时只清理本次资源。

## 安全边界

脚本拒绝覆盖已有 `.env.docker`，拒绝正式 `dwg-agent_*` 卷和非 CI 项目名，不读取生产密钥、不发布镜像、不远程部署。失败诊断只输出容器状态及有限日志，不输出临时环境文件。
